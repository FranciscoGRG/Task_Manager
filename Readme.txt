Proyecto gestion de tareas monorepo basado en microservicios para implementar AWS (No se puede por ser Java 25 y no haber imagenes docker que lo soporten).
Para desplegar el proyecto es necesario Docker. El comando para desplegarlo es "docker-compose up --build -d" desde la raíz del proyecto.

Stack para Backend:
  -Spring Boot  
  -Spring Security
  -PostgreSql
  -JPA/Hibernate
  -MinIO para almacenamiento de archivos
  -Docker
  -JWT
  -Rest Template

Stack para Frontend:
  -Angular
  -TailwindCss
